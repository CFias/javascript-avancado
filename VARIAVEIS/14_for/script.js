for (let num = 0; num < 100; num = num + 3) {
    console.log(`O resultado da soma de num + 2 é: ${num + 2}`)
}

for (let velocidade = 0; velocidade <= 80; velocidade = velocidade + 1) {
    console.log(`${velocidade}km foi a sua velocidade`)
}